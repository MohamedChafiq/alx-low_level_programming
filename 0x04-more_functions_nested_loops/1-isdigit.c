#include "main.h"
/**
 * _isdigit - Check for a digit (0 through 9)
 * @c: int to be checked
 * return: 1 if c is a digit, 0 otherwise
 * description:
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
